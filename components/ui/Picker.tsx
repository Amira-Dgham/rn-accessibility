import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

export const Picker = () => {
  return (
    <View>
      <Text>Picker</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
