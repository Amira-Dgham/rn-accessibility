import React, { FC } from 'react';
import { StyleSheet, Text, View } from 'react-native';

const compreension: FC = () => {
  return (
    <View>
      <Text>compreension</Text>
    </View>
  );
};

export default compreension;

const styles = StyleSheet.create({});
